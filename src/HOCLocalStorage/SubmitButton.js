import React from "react";
import WithLocalStorage from "./WithLocalStorage";

const SubmitButton = () => {
    return (
        <div>

        </div>
    )
}

export default WithLocalStorage(SubmitButton);